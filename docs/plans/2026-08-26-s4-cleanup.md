# 實作計畫：S4 收尾 (篩選頁 / Splash / 清理舊常數)

## 檔案異動
1. `lib/flow/splash/view/splash_page.dart`:
   - 移除 `await Future.delayed(const Duration(seconds: 3));`
2. `lib/features/foundation/style/theme_color.dart`:
   - 保留 `ThemeColor.appPrimary` 僅供 `theme_data.dart` seed 使用。
   - 移除 `ThemeColor.backBtn`，因為應該使用 `colorScheme.onPrimary`。
3. 全域替換 `ThemeColor.appPrimary` 與 `ThemeColor.backBtn`:
   - 包含 `lib/flow/settings/view/settings_page.dart`
   - `lib/flow/restaurant/view/restaurant_detail_page.dart`
   - `lib/flow/photo_viewer/view/photo_viewer.dart`
   - `lib/flow/favor/view/favor_page.dart`
   - `lib/flow/filter/view/filter_page.dart`
   - `lib/flow/main/view/main_page.dart`
   - `lib/flow/signinup/view/sign_in_page.dart`
   - 將 `ThemeColor.appPrimary` 替換為 `Theme.of(context).colorScheme.primary`
   - 將 `ThemeColor.backBtn` 替換為 `Theme.of(context).colorScheme.onPrimary` 

## 任務拆分
- **Task 1**: 修改 `lib/flow/splash/view/splash_page.dart` 移除假延遲。
- **Task 2**: 修改 `ThemeColor` 並全域替換舊的色彩常數。
